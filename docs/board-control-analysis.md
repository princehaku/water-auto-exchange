# GK21-SPTM_rev0.3 电机控制推断

日期：2026-09-05；更新至 2026-09-06。依据：用户提供的正反面照片、合宙 Air724UG 官方 GPIO 文档、无锡睿智微电子 RZ7886 原厂署名数据手册。以下是待验证假设，不是已确认的接线图。

最近验证：`0.2.4` 曾通过 `STATUS` 核对 13 路候选，并于 2026-09-06 00:23:21.084 成功 `STOP`。用户随后提供 00:31:47.904 的 13 路扫描日志并确认 GPIO12 对应板载灯。本轮读取状态时 COM4 不存在，尚未重新核实当前运行版本和状态。电机业务映射尚未完整确认，原 3 秒通/3 秒断目标尚未启用。

## GPIO5 的测量记录与后续请求

用户贴出的日志为 2026-09-06 00:21:51.634 `PROBE gpio=5 physical=49 level=1 phase=HIGH index=1/13`，并反馈该阶段出纸端万用表读数约 6.1 V。这使 GPIO5 成为出纸控制的强候选，但低电平时出纸端是否降至 0 V 尚未确认，完整映射和停止电平仍需验证。6.1 V 是用户测得的板端电压，不是 GPIO5 自身的逻辑高电平；模块 GPIO5 属于固定 1.8 V 域。

用户已澄清为只循环切换 GPIO5，供其测量板上其他测试点，高、低各 5 秒。`0.2.5` 已准备完成（电机/USB/启动测试 18/18、诊断测试 13/13、官方 LuaFLOAT 语法及 PowerShell 解析通过）：仅 GPIO5（模块物理 49 脚，固定 1.8 V 域），按 HIGH 5 秒/LOW 5 秒循环，周期 10 秒；上电自动启动，不操作其他 GPIO、任何 LDO 或物理 UART。`STOP` 可停止当前循环，下次上电再次自动开始。外接负载继续保持断开；收到准备完成通知后，由用户手动下载脚本再验证。`0.2.5` 当前尚未实测，原 13 脚 `0.2.4` 已停止，电机业务仍未启用。

## GPIO12 的板载灯记录

用户提供 2026-09-06 00:31:47.904 的日志：`PROBE gpio=12 physical=53 level=1 phase=HIGH index=5/13 hold_ms=5000 domain=V_GLOBAL_1V8`，并明确表示“这个是板子上的灯”。据此记录 GPIO12（模块物理 53 脚）对应板载灯，证据类型为用户观察；低电平时的亮灭状态和具体 LED 标号尚未单独核实。

该条是 13 路扫描的日志，不能当作 `0.2.5` 单 GPIO5 程序已经运行的证据；只更新板级功能记录，不把 GPIO12 加回当前程序。

## 第三组诊断历史（0.2.4）

`0.2.4` 按下表顺序诊断，物理脚号按官方模块定义记录；除上述 GPIO5 强候选外，未形成电机映射结论。电机/USB/启动测试 18/18、诊断测试 19/19、官方 LuaFLOAT 语法及 PowerShell 解析均通过。

| 顺序 | GPIO | 模块物理脚 | 电压域 |
| --- | --- | --- | --- |
| 1 | 5 | 49 | 固定 1.8 V |
| 2 | 9 | 52 | 固定 1.8 V |
| 3 | 10 | 54 | 固定 1.8 V |
| 4 | 11 | 55 | 固定 1.8 V |
| 5 | 12 | 53 | 固定 1.8 V |
| 6 | 17 | 50 | 固定 1.8 V |
| 7 | 20 | 3 | 固定 1.8 V |
| 8 | 21 | 4 | 固定 1.8 V |
| 9 | 0 | 41 | VLCD 档位 2，约 1.828 V |
| 10 | 1 | 58 | VLCD 档位 2，约 1.828 V |
| 11 | 2 | 40 | VLCD 档位 2，约 1.828 V |
| 12 | 3 | 39 | VLCD 档位 2，约 1.828 V |
| 13 | 4 | 57 | VLCD 档位 2，约 1.828 V |

上电自动连续扫描，每脚 LOW/HIGH/LOW 各 5 秒，一轮 195 秒（3 分 15 秒）。进入 GPIO20 前关闭物理 UART2 以复用为 GPIO，不操作 USB 控制口；GPIO0–4 前开启 VLCD 档位 2，组末或 `STOP` 时先释放 GPIO，再关闭 VLCD。此版本下次上电仍会再次自动扫描。主机 `-Continuous` 模式可附着已运行的扫描，核对 `0.2.4` 及完整 13 脚候选串，默认采集 210 秒确认进入下一轮；`STATUS` 的 `probe_total`、`probe_cycle_ms`、`probe_candidates` 用于核对实际包内容。本轮已通过 `STATUS` 核对实际版本、总数及候选列表，并在用户反馈后停止；不据此声称全部 13 脚已完成电压测量。

本组排除内部用途 GPIO7、校准 GPIO13、CP GPIO22、SIM GPIO23/29/30/31，以及用途未明确的 GPIO6/8/16。

历史区别：2026-09-05 23:58:23 的 `0.2.2` 下载发生在源文件更新完成前，打包序列仍为 GPIO27、28、24、25、26，当时诊断保持 `IDLE`。后来 `0.2.3` 的 `temp_script` 已确认为 GPIO15、14、19、18（物理脚 31、32、37、38，固定 1.8 V），且运行状态和 `STOP` 均获实测；这次不是混合打包。`tools/vendor/luatools/log/trace_2026-09-05_205909.txt` 记录 2026-09-06 00:09:11.538 `NEXT_CYCLE cycle=3`、00:09:16.552 GPIO15 HIGH、00:09:18.471 `STOPPED event=STOP`。`0.2.3` 的电机/USB/启动测试 18/18、诊断测试 13/13、官方 LuaFLOAT 语法检查及 PowerShell 解析均通过。

此前约 4 分钟后才出现扫描日志的原因已对照实证：23:36:45 已有 CME 日志，23:40:25.995 主机启动诊断时 Lua trace 同时出现。间隔来自旧版等待主机命令；`0.2.3` 已改为上电自动启动。

## 照片直接可见

- 左侧四针电机接口，上两针附近标有“出纸电机”，下两针附近标有“仓门电机”。
- 可辨认的 RZ7886 位于接口下半部附近，更贴近仓门电机区域。
- 出纸电机接口附近有“+”丝印；往上另有小型封装器件、两端器件及 R200 电阻。照片不能可靠读出该区域所有器件型号。
- 模组为 Air724UG-NFM，板号为工科物联 GK21-SPTM_rev0.3。

## 优先验证的电路假设

| 对象 | 控制方式候选 | 推断依据与限制 |
| --- | --- | --- |
| 出纸电机 | 一路 GPIO，经可能的电平转换/开关管控制单向启停 | 独立器件区域支持单向开关这一候选；“+”只表示端子极性，不能证明该端固定供电。尚未确定高边开关、低边开关或桥式驱动，也不能确定高/低有效或是否使用 PWM |
| 仓门电机 | 两路 GPIO 控制 RZ7886，再驱动电机 | RZ7886 的位置更接近仓门接口，双向驱动用途也相符；但没有逐脚连通测量证明 |

不能仅因板上有 RZ7886，就认定出纸电机一定由它控制。出纸电机采用 RZ7886 的可能性仍未排除。

## 数据手册确认的事实

[RZ7886 原厂署名数据手册（立创镜像）](https://atta.szlcsc.com/upload/public/pdf/source/20180613/C128852_BAAE7924E869DB4853C0D962680B5688.pdf) 署名无锡睿智微电子有限公司，2011.7。该链接是文档镜像，不是当前厂商官网。

RZ7886 为双向直流电机驱动器：1 脚 BI、2 脚 FI 是逻辑输入；3 脚 GND、4 脚 Vcc；5/6 脚 FO、7/8 脚 BO 是功率输出。下表描述芯片输入端，不能直接当作未知板级 GPIO 电平表。

| FI（2 脚） | BI（1 脚） | 芯片输出 |
| --- | --- | --- |
| 高 | 低 | 一个方向 |
| 低 | 高 | 相反方向 |
| 低 | 低 | 两端高阻，停止驱动 |
| 高 | 高 | 两端低电平，制动 |

[合宙 Air724UG GPIO 官方教程](https://docs.openluat.com/air724ug/luatos/app/driver/gpio/) 使用旧版 `pins.setup(pio.P0_x, initial_level)` 控制 GPIO 高低电平，并说明 GPIO 分属不同电压域。`pio.P0_18` 表示 GPIO18，与这块定制板的 TP18 丝印不存在已知对应关系。合宙模组手册不能替代该定制 PCB 的原理图。

## 尚缺的证据

### 新近照的定位结果

用户补充了 `codex-clipboard-246008b2-4375-4529-9f1c-ab3892face8e.jpg`。该图的模组文字水平、SIM 在左、LTE 天线在右、电机接口在下，与[官方硬件手册 V3.7](https://docs.openluat.com/air724ug/product/file/Air724UG_%E7%A1%AC%E4%BB%B6%E8%AE%BE%E8%AE%A1%E6%89%8B%E5%86%8C_V3.7.pdf)第 11 页 Top View 方向一致。已查看该页引脚图。

- 官方 LTE_ANT 为物理 46 脚，可结合 SIM/USB 等接口定位模块方向；天线本身不提供电机控制 GPIO 的信息。
- 左边从上到下为物理 1–17；下边从左到右为 18–34；右边从下到上为 35–51；上边从右到左为 52–68。
- 下边可复用 GPIO 的物理脚映射：25→GPIO27、26→GPIO28、27→GPIO24、29→GPIO25、30→GPIO26、31→GPIO15、32→GPIO14。这里仅列出官方模块定义，不表示这些脚已确认连接电机。
- RZ7886 上侧右边两脚附近有细线朝模块下排延伸；在元件附近与接近焊盘的转折处，照片不足以连续追通。因此没有形成具体 GPIO 候选对，更没有确认有效电平。
- 新近照与最初正面照的模组个体标签不同，不能自动把旧背面照片中的线路、TP 位置当作当前这块板的接线。后续需使用同一块板的正反面照片。

### 待确认的接线

最新背面近照 `codex-clipboard-3466744b-5afe-4061-90f5-568e854af791.jpg` 可见 TP20、TP19 分别通过短线接到小过孔，但换层后路径不可见；TP18、TP29 没有可完整追踪的表层出线，不能由此判断悬空或接地。没有确认任何 TP 到模组 GPIO 或 RZ7886 输入的完整连接。左缘天线座粗焊点可用于正反面对齐；右下缘粗焊点不可误认成 RZ7886 引脚。

用户随后报告以下对应关系，先按用户报告记录；本次断电、拔掉电机的条件及完整四针顺序仍待回复确认：

| 测试点 | 用户报告连接位置 |
| --- | --- |
| TP18、TP29 | 出纸电机 − |
| TP19 | 出纸电机 + |
| TP20 | 仓门电机，具体端子待确认 |
| TP21 | “中间第三根没有标的” |

若在完全断电、拔掉电机且低阻直通条件下成立，这些对应关系首先标识电机端子所在网络，仍不提供控制 GPIO 编号。

用户再补充：出纸负端与电源输入负极测量时蜂鸣。此报告支持出纸负端共地，因而正极一侧的开关/供电使能更值得优先追踪；但用户尚未明确本次断电、拔掉电机条件和具体阻值，蜂鸣本身不能排除经绕组、低值电阻或半导体器件导通。暂不把“固定负极、高边开关”标为已验证拓扑，控制 GPIO 仍未知。先前“固定正极、切换负端”的照片假设不再作为优先方向。

继续按断电、拔电机的测量步骤，用户报告 TP19 到电源输入正极约 30 kΩ；交换两支表笔后读数相同。按报告记录为两方向均未测到近零欧姆直通，不能据此单独确认开关拓扑，也不能推断电机直接使用输入 12V。

此前安排的连通测量目标：正面天线向右、电机接口向下时，RZ7886 左侧竖放棕色电容再往左的黑色三脚器件，其朝向电机插座的独立一脚。该器件目前只是出纸开关候选，型号和真实引脚编号尚未确认。拟在完全断电、拔电机后测 TP19 至该脚的实际阻值并交换表笔复测；尚未获得这项测量结果。另已开展下述受限 GPIO 电压诊断。

- 出纸插座两个板端针脚分别通向哪个功率器件。
- 该器件控制端是否经中间器件连接模组，以及最终 GPIO 编号和有效电平。
- 电机的实际供电电压。板上 +12V 输入标记不能证明电机直接使用 12V。
- TP18、TP29 的实际网络。用户的蜂鸣/约 0.8 MΩ 报告来自未断电且档位不清的测量，不能作为连通性证据。

已搜索板号、工科物联、SZGKWL 以及 GitHub/Gitee，未找到可复核的该板公开原理图、引脚表或应用源码；这不表示厂商没有内部资料。

## 对现有程序的影响

3 秒通、3 秒断的循环逻辑已经实现，但电机业务配置仍为 `enabled=false`、`mapping_confirmed=false`，`START` 会拒绝启动。GPIO5 高电平阶段的 6.1 V 报告尚不足以确定完整启停映射，低电平响应仍待观察。现有电机控制模板按双输入驱动准备；若确认出纸采用单路开关，需相应调整驱动部分。当前 `0.2.4` 已实测并停止；仅 GPIO5 高/低各 5 秒的 `0.2.5` 已准备完成，尚未上板验证。

“USB 插座—出纸电机—RZ7886”区域的器件字样、走线和断电连通性仍是需要补充的证据；受限诊断获得的电压变化也须与具体测量端子对应，不能仅凭程序日志认定电机接线。

## 对“轮询引脚定位”的判断

- 周期读取 0/1 不能识别外接器件身份，不能自动找出电机控制脚。
- 当前 LuaTask V2.4.4 的 `pins.setup(pin)` 会调用 `pio.pin.close` 并设置为输入；已配置输出后，返回函数无参数调用也会切回输入。因此不能把这种遍历当作完全保持硬件状态的读取。
- 原生 `pio.pin.getval` 在 Lua 层没有附加配置操作，但尚未确认其对未初始化、外设复用或未供电电压域的引脚能否可靠反映实际电平；也未找到可完整保存和恢复 pinmux/方向/电压域的公开接口。
- 逐脚拉高/拉低属于主动电气测试。全 GPIO 扫描可能改变启动/通信/板上外设状态或与已有输出冲突，不能只靠缩短脉冲消除这些问题。
- 受限候选测试是可用的调试方法，但前提是确认候选的用途、电压域、是否存在外部驱动及停止方式；试验时先断开电机和其它负载，用直流电压档观察输出响应。仅拔掉电机不等于其余电路已可随意扫描。
- 第一组 GPIO24–28 与第二组 GPIO15/14/19/18 均已执行受限诊断，用户均未观察到电压变化，不能排除使能组合。第三组 GPIO5 的高电平阶段获得出纸端约 6.1 V 的用户反馈，暂列强候选；完整映射仍未确认。

接口依据：`tools/vendor/luatools/resource/8910_script/script_LuaTask_V2.4.4/lib/pins.lua` 及[合宙官方 GPIO 教程](https://docs.openluat.com/air724ug/luatos/app/driver/gpio/)。

## 首组受限诊断的实施记录（0.2.0 / 0.2.1）

用户已明确确认：电机、人体红外、NFC、LED、键盘等所有外接负载均已拔掉，仅连接电源和 USB；万用表使用直流电压档（DC V）。这次观察的是板端电压对候选 GPIO 电平的响应，测试本身不确认电机 GPIO 映射。

`0.2.0` / `0.2.1` 的 `src/gpio_probe.lua` 一轮仅按以下顺序测试，每脚 LOW 5 秒、HIGH 5 秒、LOW 5 秒，总计约 75 秒。每 5 秒的日志包含 GPIO 编号、模块物理脚号和当前电平。

| 顺序 | GPIO | 模块物理脚 |
| --- | --- | --- |
| 1 | GPIO27 | 25 |
| 2 | GPIO28 | 26 |
| 3 | GPIO24 | 27 |
| 4 | GPIO25 | 29 |
| 5 | GPIO26 | 30 |

这两个历史版本的诊断使用 VMMC 档位 13，名义电压约 3.054 V；结束或停止时释放当前 GPIO，再关闭本次启用的 VMMC。`0.2.1` 的连续模式需显式启动；`0.2.3` 后来改为固定 1.8 V 第二组及上电自动扫描。

2026-09-05 23:16:55，LuaTools 报告 `0.2.0` 下载成功；随后 COM4 `STATUS` 已识别 `version=0.2.0`、`probe_state=IDLE`。准备期间的“缺 sys”提示来自 LuaTools 对同一行两个 `require` 的依赖误解析：`sys, pins = require "sys", require "pins"` 被当作文件名 `sys", require "pins.lua`。代码已拆成两行；`sys.lua` 本就在默认库内，无需额外复制。官方 LuaFLOAT 语法检查通过，`tests/motor_test.lua` 16/16、`tests/gpio_probe_test.lua` 12/12 通过。

23:20:45.307，通过 `tools/gpio-probe.ps1 -Port COM4` 启动第一轮，按 GPIO27、28、24、25、26 顺序收到全部 15 条 LOW/HIGH/LOW 阶段日志。23:22:00.346 返回 `PROBE state=DONE event=END total=5 duration_ms=75000`；23:22:01.344 的 `STATUS` 为 `probe_state=DONE probe_gpio=none`；主机最后发送 `STOP`，23:22:01.405 返回 `OK STOP stopped`，工具退出码为 0。日志为 `logs/gpio-probe-20260905-232045.log`。

首轮已停止，没有自动重复。日志证明诊断程序完成预定序列并停止，不能单凭日志认定板端实际电压或电机控制 GPIO。`tools/motor-command.ps1 -Port COM4 -Command STOP` 可停止诊断，执行前须先让占用同一 COM4 的工具释放串口。

用户随后明确要求持续运行，已使用 `tools/gpio-probe.ps1 -Port COM4 -Continuous` 启动 `0.2.1` 的 `PROBE LOOP`。`logs/gpio-probe-20260905-234025.log` 记录：23:40:25.944 的 `STATUS` 实际确认 `version=0.2.1`；23:40:25.995 启动连续模式；23:41:41.046 出现 `PROBE state=RUNNING event=NEXT_CYCLE cycle=2`。23:41:56.002，主机工具退出时 `STATUS` 仍为 `probe_state=RUNNING probe_continuous=1 probe_cycle=2`，板端继续运行。主机工具退出不等于连续诊断已停止。

后续本轮通过 `tools/motor-command.ps1` 查询到 `probe_state=RUNNING probe_gpio=25 probe_level=0 probe_continuous=1 probe_cycle=11`，随后发送 `STOP` 并收到成功应答，连续诊断已停止。用户反馈 GPIO24、25、26、27、28 测试时均未观察到电压变化。该结果只覆盖此次逐脚 LOW/HIGH/LOW 的测量条件，不足以排除这些脚参与组合控制、额外使能或其它供电条件；电机 GPIO 与有效电平仍未确认。

第二组 `0.2.3` 和第三组 `0.2.4` 的实测结果及后续请求见文首记录。
