# Web 控制台与 4G 接入

## 2026-09-13 最新：移除主界面停止全部输出按钮

主界面移除“停止全部输出”，底部保留占满一行的“按目标水位”入口。补水和排水继续由各自开关独立关闭；目标作业在详情弹窗内使用“停止目标任务”。本次只改网页展示，STOP协议、服务端限时与0.8.2固件均保持不变。

桌面及390/360px手机的现有目标作业浏览器定向通过。已静态部署，备份`/apps/water-auto-exchange/backups/20260913T100406Z-web-101740`，公网9项资源一致、API健康，服务未重启。

## 2026-09-13：0.8.2 生产固件的 Web 估算与多轮目标作业

实板继续使用 0.8.2，本轮只修改网页与服务端。用户实测排水 300 秒水位下降约 10 个百分点，满到空校准用时应为 `300 / 0.10 = 3000` 秒（50 分钟）。补水用时须单独测量，当前水位以现场观察为锚点。校准时间描述完整水位跨度，与单轮 3/5 分钟的控制上限不同。

0.8.2 每秒活动心跳仅含序号，完整状态只在变化时上报；旧服务端因 12 秒没有新输出样本而停止估算。兼容修复以已确认开启状态、当前控制授权和连续有效心跳支持时间估算，并保持真实设备状态报告时间独立。页面百分比显示至一位小数。估算不能充当水位传感器；未知状态、活动通信缺口、断联或重启后不补算缺失区间，须重新校准。

`POST /water/api/level-job` 接受 `target_level`（0–100），按当前可信水位和校准速率选择补水或排水。单轮补水最多 170 秒、排水最多 290 秒，给既有 180/300 秒软上限预留 10 秒；每轮发送普通独立 OFF，收到成功回执并确认全关后间隔 2 秒再开始。最后一轮按剩余目标用时缩短。`POST /water/api/level-job/cancel` 结束作业并停止输出，`GET /water/api/status` 的 `level_job` 提供目标、方向、轮数和累计估算进度。服务端执行任务，关闭网页后仍继续；设备断联、故障、输出不确定或服务重启时结束，不自动恢复。

用户追加要求板端代码也回滚到0.8.2。固件源码和生成包以已发布的`3020034`为准，下载项目恢复`water-online-0.8.2`，以后确有需要再修改板端。0.8.3板端改动回滚，未发布的0.8.4方案撤销；实板本来已是0.8.2，无需刷写。Web修复与分轮作业不依赖新固件，不发真实输出命令，不用本地模拟试验替代实际水位验收。

0.8.2普通手动开关达到服务端180/300秒软上限时，改发正常STOP以避免内部TIMEOUT触发固件FAULT；只接受本轮有效停止回执并确认全关IDLE后清理时限。确认失败仍结束连接，固件执行5秒失联保护。已有真实FAULT继续保留，不自动RESET。0.8.3原内部到时命令兼容保留。

验证：181项Python、完整浏览器及最终校准竞态定向通过。真实本地0.8.2裸ping驱动1000秒四轮排水，累计20L（测试容量60L、排空3000秒）；确认关阀/2秒间隔、关闭网页继续、取消和断联不续开、待执行ON禁止校准、溢水或不就绪不得开下一轮、082普通软限STOP和083兼容均覆盖。桌面、390px和360px手机保持一屏。没有操作真实GPIO或生产控制命令。

已部署，备份`/apps/water-auto-exchange/backups/20260913T094838Z-96051`。公网9项静态资源与本地一致，water-console健康/restart=0，TLS/WSS凭据probe、HTTPS登录/status/退出、999天会话、level_job/估算字段及water/sms健康通过。已有校准和不确定标记保留，需按现场水位重新校准后开始目标任务。

板端随后按用户要求回滚完成：src与Lua测试精确匹配已发布0.8.2提交`3020034`，227项Lua回归通过，8Lua+CA生成包、原CORE与LuaFLOAT解析核验通过。没有刷写生产实板。17:50只读API显示用户已重新保存校准，uncertain=false且有补排水估算累计；以用户最新保存值为准，不由助手覆盖。

## 2026-09-13：0.8.3 补排水到时自动关闭，不锁存故障

用户明确补水、排水超时只自动关闭，不要故障锁定，覆盖此前正常时限到期后FAULT/RESET约定。0.8.3正常fill_timeout/drain_timeout关闭全部并回到IDLE，保留到时原因，可直接再次开启。联网手动仍由服务端执行补水180秒、排水300秒，USB本地与自动模式的正常阶段到期也回到IDLE。每次开启仍须新命令，不自动续开；重复ON、操作另一路或刷新网页不延期。

服务端新增0.8.3支持，正常超时确认后清理本轮计时并允许新授权；旧OFF、旧已终结或迟到ACK不能伪造本轮关闭。0.8.2仍兼容原FAULT回执，0.8.1及更早版保持原板端限时，不能把网页升级当作实板行为已改变。5秒通信保护、溢水和IO异常保持原关断规则；正常到时命令不能清除已存在的故障，真实关断失败也不伪造IDLE。

网页到时显示“补水/排水到时已关闭，可再次开启”，状态正常且开关可用，无需复位。源码与下载项目升至water-online-0.8.3，完整8Lua+CA，保留原CORE与默认库。新行为需下载固件后实板生效，本轮不刷板、不发送真实输出命令。

16:57只读API确认实板已更新为0.8.2，在线、FAULT/drain_timeout、fill=0/drain=0。此前0.8.0是旧快照；本次正针对正常超时锁定行为修正，新行为需下载0.8.3。

本地234项Lua、165项Python及082/083桌面手机浏览器定向联调通过，覆盖正常到时IDLE后直接重开、旧超时回执不影响新作业、到点与新ON同事件先LOW+close、STOP取消新ON、关断IO失败仍FAULT、真实WSS往返和旧版本兼容。测试未操作真实GPIO。

服务端与网页已部署，备份`/apps/water-auto-exchange/backups/20260913T085949Z-78402`；容器healthy/restart=0，公网9项静态资源一致，TLS/WSS凭据probe、HTTPS登录/status/退出、999天会话及water/sms健康通过。下载包0.8.3的8Lua+CA、源码一致性、原CORE与LuaFLOAT语法已核对；本轮未刷板、未发真实输出或RESET命令。

## 2026-09-13：0.8.2 服务端软上限与固件5秒失联保护

用户指定“固件断联最多5秒，否则以Web端软上限为准”，覆盖0.8.1联网手动控制的板端固定时限。0.8.2/manual由服务端从每路开启授权开始计时，补水180秒、排水300秒；重复ON、切换另一路、关闭网页、水位未校准都不延期。计时与水位估算独立，以单调时钟判断截止时间。网页只展示服务端倒计时及设备回执，不在浏览器中安排停止，也不以零秒伪造关闭。

任一路到期，服务端发送内部FILL_TIMEOUT/DRAIN_TIMEOUT命令，使固件关闭全部并锁存fill_timeout/drain_timeout，需RESET；这些内部命令不对网页API或USB开放。1秒内未确认全关故障状态，服务端终止会话并停止有效pong，交给板端5秒失联保护。旧OFF报告不能撤销已授权但未收到ON回执的计时；未知输出、无对应授权的开启或会话重建不继承旧计时与输出。

固件控制器独立执行5秒通信期限，不受socket的30秒发送等待阻塞；首次远程开启建立期限，之后只有当前会话新鲜有效pong续期。重复ON、另一路开启、received或迟到/重复pong均不延长已有期限。显式断开仍立即关断，重连不自动开水。握手必须确认服务端180/300秒软上限及5秒保护能力，避免与旧服务端混用。USB本地控制与自动模式保留板端180/300秒限时；远程自动模式也受独立5秒通信保护。0.8.2手动远程控制必须使用WSS，不使用旧HTTP到USB网关。

兼容旧固件：0.8.1仍按板端180/300秒，0.8.0及更早版仍按原120秒；网页不会提前宣称实板已采用新机制。源码/下载项目版本0.8.2，整包仍为8Lua+CA，保留原CORE和默认库。需要用户下载新固件后板端机制才生效；本轮不刷写、不发送真实输出命令。

本地验证通过227项Lua与136项Python回归，覆盖socket等待时5秒关断、服务端无浏览器计时、3/5分钟边界、重复/迟到回执、会话隔离及真实本地WS握手。旧OFF回执不能撤销新的开启授权；回执按本会话execute顺序验证并只消费一次。测试均未操作真实GPIO。

桌面/手机浏览器联调及旧版倒计时兼容通过；短屏360×640在显示离线反馈时仍无裁切。服务端/网页已部署，备份`/apps/water-auto-exchange/backups/20260913T054915Z-16559`；容器healthy/restart=0，公网9项静态资源一致，TLS/WSS凭据probe、HTTPS登录/status/退出、999天会话、水量与control_limits字段、water/sms健康均通过。部署前13:49只读API显示设备离线，最近12:56:36上报0.8.0/IDLE/fill=0/drain=0；不能将历史状态当作当前实物状态。没有注册生产模拟设备、刷板或发送真实开水指令。

## 2026-09-13：0.8.1 补水3分钟、排水5分钟与倒计时

用户指定补水最长3分钟、排水最长5分钟，覆盖此前两路120秒约定。0.8.1 的 `water_config` 与状态机默认值分别为补水180000ms、排水300000ms。每路从实际开启计时；重复ON或开关另一路不延期。任一路到期仍关闭全部并锁存FAULT，需RESET；10秒活动失联、溢水、IO异常及LOW后release关断规则保留。

网页每路新增“预计剩余”倒计时，每秒本地更新，HTTP查询仍每2秒；原满水/排空时间、流速与补排水量预估继续显示。保护倒计时来自服务端首次观测开启时间，可能晚于物理启动，只是展示；不会自行发送STOP或以零秒伪造关闭状态。未校准水位也可显示有效开启计时；离线、API中断、未知输出或计时不确定时隐藏倒计时。0.8.1显示补180秒/排300秒，0.8.0及既有0.7版本仍显示原120秒，避免网页先于板端变更。

源码与生成包已升0.8.1，下载项目为 `water-online-0.8.1`，仍8Lua+CA，保留原CORE与默认库。当前实板最近API为0.8.0；新上限需下载0.8.1后生效，本轮未刷板或发送真实输出命令。服务端、网页和USB工具同时兼容0.8.0/0.8.1的并行手动模式，旧版与自动模式限制保留。

## 2026-09-13：0.8.0允许同时补水和放水

用户明确要求“允许一边放水一边补水”，覆盖此前手动模式两路互锁约定。0.8.0/manual允许GPIO23补水与GPIO5排水同时开启，状态为EXCHANGING；FILL/DRAIN是各路幂等开启，新增FILL_OFF/DRAIN_OFF独立关闭，STOP关闭全部。切换一路不写入或释放另一条已开启的输出；所有关断仍采用LOW后pins.close。automatic模式仍按原先排水、等待、补水流程运行。

每路独立记录实际开启时刻，各最多120秒；开启另一路或重复ON不刷新既有计时。任一路到期仍沿用全局FAULT/关闭全部/需RESET的语义，另一条可能提前停止。溢水、IO故障及远程10秒失联关闭全部，不在重连后恢复。独立OFF取消对应尚未执行的ON；STOP取消全部待执行命令。独立关断或定时器清理失败后禁止无轮询重启，需重启板子恢复。网页两个开关各关各路，保留“停止全部输出”；仅对0.8.0/manual启用并行能力，旧固件保留互锁和STOP关闭语义，后端/USB工具有同样版本检查。

保留待机30秒/活动1秒心跳、5分钟流量上报；EXCHANGING也按活动状态使用10秒失联期限。用户追加LED要求：任意一路开启时GPIO12常亮；WSS认证成功且待机时，每次收到有效新pong短亮200ms，正常约30秒一次；未联网、认证失败或断线时恢复LuaTask V2.4.4原网络闪烁。常亮优先于网络灯效。只响应有效回执，不对仅入队ping、重复/过期/旧会话pong闪灯。断线/开始工作会作废短闪定时器，旧回调不能熄灭工作灯或覆盖断线闪烁；LED回调/定时器异常不阻断控制和联网。

本轮01:01日志和真实API确认实板已是0.7.7/manual/IDLE/ready=1/outputs_known=1、两路为0，5分钟统计已上报，待机seq/ack按30秒增长；替代上一节0.7.7待下载快照。没有由助手刷写或操作真实输出，0.8.0并行输出仍需用户下载实测。

本地208项Lua（12+15+22+41+26+56+36）、78项Python以及Edge桌面/手机真实本地HTTP联调通过；覆盖双开/任一路关断、另一输出无额外GPIO写入、独立计时/重复ON不延期、双开超时/溢水/断网关闭、迟到指令与回调、关断/定时器失败、LED保留、WSS命令回执和旧固件拒绝并行。测试未操作真实GPIO，不能代替双路负载实测。源码/生成包0.8.0，下载项目water-online-0.8.0，原CORE和默认库不变，仍8Lua+CA。

0.8.0兼容后端/网页已部署，备份`/apps/water-auto-exchange/backups/20260912T170432Z-3958983`。部署前01:04确认真实0.7.7/IDLE、fill=0/drain=0。容器healthy/restart=0，公网四静态文件与本地一致，TLS/WSS凭据probe、HTTPS登录/status/退出、999天会话和water/sms健康通过；没有向生产注册模拟设备或发开水命令。生成包9项、LuaFLOAT/JS/PowerShell语法、源码/版本/联网凭据/120秒上限/30秒与1秒心跳/300秒统计/原CORE核对通过。未恢复此前Escape停止的桌面输入，0.8.0仍需用户下载。

## 2026-09-13：0.7.7省流量与工作常亮灯

用户确认采用待机30秒、工作1秒心跳、每5分钟上报流量，覆盖此前空闲也每秒心跳的要求。`src/water_network_config.lua`设置`heartbeat_ms=30000`、`active_heartbeat_ms=1000`、`traffic_interval_s=300`；统计周期按秒传给当前LuaTask V2.4.4的`socket.setIpStatis`，允许60–3600整数秒，省略时默认300。无统计API仍可联网。

WSS持续保持，服务端推送命令不等下一次心跳；执行命令时刷新活动通信计时，工作期间仍每秒心跳。状态变化下个500ms网络检查立即上报，USB/trace仍每5秒打印；流量上报变慢不延长活动10秒失联停止、75秒空闲检测或单次120秒输出上限。120秒到期关断并锁存`fill_timeout`/`drain_timeout`，需要复位；重连不恢复输出。网页仍每2秒查询服务器，流量数字取最新已上报样本。

GPIO12/物理53由`netLed`单独控制；真实输出记录明确开启时，所有网络状态（包括未联网/搜网）都设为常亮，停止后恢复V2.4.4原有各状态闪烁时长。复用`updateBlinkTime(state,65535,0)`，不另启GPIO写入任务与库争用。控制器在应用输出后通知灯效，异常被隔离，不能阻止STOP；USB独立控制也能亮灯。常亮反映软件输出记录，不是水流或电压反馈。灯闪烁与WSS心跳独立，不消耗SIM数据。

截至00:17，真实日志/API已确认0.7.6、流量持续上报；旧卡联通（3gnet），新卡移动（CMIOT），23:51:26重启由用户换卡触发。移动卡00:03:41上线后至00:17仍有通信；此前两卡均出现过超时，短时稳定不能证明网络问题永久解决。00:17真实状态为FAULT/drain_timeout、fill=0/drain=0。

旧配置两段60秒样本12844/12959字节，折算约557MB/30天，属于库估算而非运营商账单。新配置按每天工作10分钟、其余待机，模型约25–40MB/30天；重连、TLS和重传会影响实际值。此数不是0.7.7实测节省。5分钟未上报区间在掉电时可能丢失。

源码/生成包0.7.7，下载项目`water-online-0.7.7`，仍8Lua+CA、原CORE与默认库。LED及省流量需要用户下载脚本；本轮没有恢复Escape停止的桌面输入或操作真实输出。

本地195项Lua（12+15+20+36+23+53+36）、72项Python及Edge桌面/手机联调通过；覆盖30秒待机边界、服务端主动命令、活动1秒心跳与10秒停止、5分钟统计、75秒空闲期限、GPIO灯效切换/超时恢复及灯效异常不阻断STOP。LuaFLOAT、JS、PowerShell语法通过；9项生成包/版本/源码/私有凭据/超时/输出映射/原CORE核对通过。测试未操作真实GPIO，不等同0.7.7实板验证。

后端/网页0.7.7兼容已部署，备份`/apps/water-auto-exchange/backups/20260912T161911Z-3944482`；容器healthy/restart=0，公网四静态文件与本地一致，TLS/WSS凭据probe、HTTPS登录/status/退出、999天会话和water/sms健康通过。部署前确认真实两路均关闭；本轮未注册模拟设备或发送开水命令。实板仍需用户下载0.7.7，服务端更新不会改变板端心跳或灯效。

## 2026-09-13：三维控制台为唯一入口，一屏布局

`/water/` 与 `/water/aquarium.html` 现在提供同一个三维控制台，经典页面及其专用 app.js/style.css 已移除。主界面保留三维鱼缸、模拟水位与水量、补水/冲水开关、运行进度、紧凑水量预估和“按目标水位”入口。顶部“设备状态、历史记录、水位校准”分别打开可关闭的弹窗，详情、故障复位和退出位于设备状态内；历史保留操作/在线离线两个页签。弹窗内容独立滚动，支持关闭按钮和 Escape，打开时继续每 2 秒自动同步。

顶部只有一个设备概况指示器：在线、就绪、输出已知且无故障/溢水时显示正常；设备离线、故障、未就绪或输出未知显示异常；浏览器与服务失去连接时显示“状态未知”，不会误认设备离线。手机将两个开关并排、鱼缸与控制上下排列；桌面并列显示，主界面按可用视口高度排布。0.8.0/0.8.1/0.8.2/0.8.3 的 manual 模式支持两路同时开启、分别关闭，旧固件保持互锁及 STOP 语义。开关仍以设备回执为准。

网页静态更新使用 `deploy/publish-web.sh`：将含 deploy/www、deploy/install.sh、deploy/publish-web.sh 和 docs/web-console.md 的 web-update.tar.gz 上传至项目根目录，执行脚本。发布前备份线上及部署源文件，发布后核对文件并清理两个已退役的经典资源；失败恢复备份，过程不重启 API、容器或 Nginx。

手动模式没有真实水位传感器。新页面默认鱼缸水面是**场景示意**，不显示百分比。用户需根据过去实际经验填写“空到满”和“满到空”的时长，并在设备在线、两路均关闭且现场水位已知时设置当前水位（满水 100%、空水 0% 或已知百分比）。校准保存在服务器 SQLite，而非单个浏览器；之后服务端按设备确认的各路开启时间积分，两路同时开启时按净变化计算，并在各浏览器显示同一模拟水位。0.8.2/0.8.3在当前会话已确认授权开启、输出已知且活动心跳持续有效时，可按最后回执状态外推，完整状态报告时间另行保留；其他报告模式仍需连续新鲜输出样本。首次安装前的历史命令没有完整输出变化时间戳，不能倒推出真实水位或自动回填校准。工作期间通信断开、状态未知或服务重启后输出时刻不明，估算标为不确定，需重新查看现场水位再校准。手动开关不会因模拟达到0%/100%而停止；只有用户显式启动的目标水位作业按估算目标收尾。每轮补排水保护计时独立于水位估算，0.8.2/0.8.3联网手动控制由服务端限时，更早版由板端限时。

三维渲染使用随站点部署的 Three.js 模块和许可证；不依赖第三方 CDN。WebGL 不可用时仍可使用二维控制与历史区。

### 水量校准与补排水预估

校准可额外填写满水时的有效容量（0.1–100000 L），指实际盛水量，不根据三维模型的比例猜测。未填写容量仍可按百分比模拟水位，所有升数和 L/min 显示未知。配置补水流速 = 容量 × 60 / 空到满用时，排水流速 = 容量 × 60 / 满到空用时；当前水量 = 容量 × 模拟水位百分比。流速是由用户历史用时得到的标定值，不是水流传感器读数。

主页显示每路本次估算水量、净流量，以及按当前开关保持状态推算的满水或排空时间。两路同时开启分别累计补水与排水，净流量取两者之差；净变化为零时不显示满空倒计时。普通手动开关的满水/排空预计时间不安排停止；每路补排水保护计时独立执行，不受水位估算影响。手动达到 0%/100% 只截断缸内水位，阀门本次累计仍按可信运行区间增加，不能把累计注入量当成缸内现存量。“按目标水位”需要用户另行启动，会分轮执行到指定估算水位后关闭。

每路“本次”在该路首次确认开启时单独归零，重复开启报告不会归零，关闭后保留直到该路下次开启。校准菜单显示从本次校准以来的补排水累计及校准时间；重新保存校准会清零本次与累计。容量、校准与统计持久化于服务器，刷新页面或换浏览器不会从零开始；旧版本记录升级时不推算此前缺失的累计水量。

累计依据完整输出回执，以及0.8.2/0.8.3当前授权开启期间持续有效的心跳。后者只是已知输出状态的时间估算，不是新的输出传感器测量。浏览器不自行积分，也不从控制命令的提交时间补算。工作中失联、输出未知或有效通信断档时暂停；服务连接中断时保留上次水量并隐藏实时净流量和预计时间。估算不确定后需核实现地水位重新校准。本轮只统计阀门补水和排水，不包括自然蒸发、渗漏或缸内自循环。

### 实物龟缸场景

按用户提供的俯视、正面和 45° 三张照片重建整体结构：左侧低位玻璃龟缸，右侧抬高的土壤陆地区；中部以粗岩岸、鹅卵石浅水道连接小水池和左侧水区。保留两只红耳巴西龟、橙色爬坡、棕色围板与立柱、绿色管路和黑色接头/橙色喷嘴。模型按照片估计比例，未测量实际尺寸；照片本身不加入公开静态资源。

场景默认保留 45° 视角，取消固定视角按钮。鼠标按住拖动或手机单指拖动可旋转，双击恢复默认；键盘聚焦画布后可用方向键旋转、Home 复位。松手、状态刷新和窗口尺寸变化都保留当前朝向，俯仰范围限制在底座上方，避免翻转至缸底；仅画布接收旋转手势，不影响控制按钮和菜单。

两只龟使用独立于设备控制的场景动画：绕水区游动、划水转向，轮流经坡道上岸、在前侧土路爬行和停驻，再回到水中。窄坡错开通行；低水位时改为池底爬行；后台页面和系统减少动态效果时暂停行走时钟，恢复后不跳过路线。坡道适度加宽并清出出口，内部隔板缩短以便通行，外侧完整透明围板保留。动画模块随站点本地部署，无外部资源依赖。

左侧白色管按用户确认设为排水管，从缸底吸水端向上排出；旁边新增独立绿色补水管，管口位于水面上方。补水动画从绿色管口落入水面，排水动画沿白色管向上移动；两路根据各自已知输出单独显示，可同时显示，关闭或通信中断后停止对应动画。后侧绿色喷花管和陆地区自循环保持独立。

水位模拟仅改变左侧龟缸的水面。常态缸内循环作为独立的水路示意，暂按照片推定从右侧小池沿水道流回左池，实际方向尚未确认；不产生净补排水量，不占用 FILL/DRAIN 输出，也不新增设备命令。乌龟活动与循环泵都没有真实反馈：动物位置和水波动画仅作场景表现。喷花仅还原管路及关闭的喷嘴，不提供喷花开关。补排水动态仍根据在线设备的已知输出显示，通信中断时停止显示对应流动。

移除经典页面后，设备估算流量的累计、最近运行、统计段和上报时间保留在顶部“设备状态”窗口；没有样本显示尚未上报，仍与运营商计费区分。

## 2026-09-12 当前：网页自动同步与恢复

登录后，页面可见时每2秒自动查询服务器，更新设备在线/离线、开关状态、命令回执、连接记录及最新已上报流量；不用反复点“刷新”。“连接与流量”右上角显示自动更新间隔及本次网页同步时间。此时间与设备“最近通信”分开，网页同步成功不意味着设备刚上报。板端流量仍约60秒统计一次，网页查询不向板子追加请求。

首次加载暂时失败也继续自动重试；恢复网络、重新聚焦窗口或切回页面时立即尝试同步，已有请求进行中时复用等待，避免重叠。后台页面暂停定时查询。每次请求最多等待6秒，失败显示正在自动重试，恢复后更新提示。明确401或主动退出才停止自动查询并显示登录；退出会取消并作废在途查询，迟到的成功响应不能重新显示控制台。手动“刷新”仍可立即查询。

Edge通过真实本地HTTP与模拟设备验证，全程0次点击“刷新”：覆盖版本与模式限制、两路开关回执、在线/离线、流量增加、初次及后续API故障恢复、慢请求不堆积、退出竞态、桌面及手机布局。本次只改网页，无需刷板，不代表0.7.6实板稳定性或流量已验证。

## 2026-09-12 当前：0.7.6连接详情与设备估算流量

连接与流量区域默认展开，显示当前状态开始时间、持续时长、最后通信距今多久以及最近60次在线/离线变化。离线时保留最近通信时间，服务重启也保留历史；服务器无法连接时显示“服务连接中断 / 设备状态未知”，不会假装确认设备已经离线，开水按钮禁用。

流量显示累计已上报、本次运行及最近统计段；约每60秒刷新。旧固件/无报告显示未知，需要下载water-online-0.7.6才有板端报告。使用官方旧库socket.setIpStatis的估算值，包含固定协议开销估算，与运营商账单可能不同。统计从启用功能开始，不补造历史流量；数据上报后持久保存，重复报告和重连不重复计数，重启前未上报数据可能丢失。

0.7.6也放宽认证等待至30秒，并清理超过原期限的旧会话占用，健康连接继续复用。实板最新保存日志已是0.7.5；新生成包仍8Lua+CA、保留原CORE及默认库，需要用户下载0.7.6。189项Lua、70项Python及桌面/手机浏览器联调通过；新版本实板稳定性和计数仍待验证。

## 2026-09-12 当前：0.7.5发送修正待下载，实板0.7.4就绪

用户17:22日志确认0.7.4/manual/IDLE/ready=1/outputs_known=1、两路为0，且出现WATER WS online；随后底层发送超时。输出映射已经生效，网页开启仍要求设备当时在线。0.7.5保留GPIO23补水、GPIO5排水及LOW后释放的关断流程，改为默认30秒发送等待、合并积压心跳、发送帧之间收包，并增加发送进度及关闭原因诊断。活动输出10秒失联停止保持不变。详见[网络恢复](network-recovery.md)。

下载目标water-online-0.7.5，完整8Lua+CA来自build/firmware。原CORE与默认库保留；本轮未恢复此前被Escape停止的computer-use，由用户点击下载脚本。183项Lua、58项后端和Edge本地开关联调通过，0.7.5实板稳定性仍待验证。以下记录保留各次调查的历史状态。

## 2026-09-12 当前：0.7.4 两路输出已配置，待下载

用户确认补水接顶部DO2、排水接出纸口，并补充旧扫描切换GPIO后接口没电压。0.7.4按该完整操作序列配置补水GPIO23、排水GPIO5：开启时配置输出并拉高；关闭时写低后pins.close释放，重新开启时恢复输出模式。开机先关闭两路，500ms状态稳定后可接受手动FILL/DRAIN；STOP、120秒超时、互锁与远程失联停止保留，无液位输入要求。详见[输出控制](water-control.md)。

源码与build/firmware已为0.7.4，LuaTools项目water-online-0.7.4，清单仍8Lua+CA共9项。实板最近已确认版本0.7.3，本版待下载；新包成功运行后应看到manual、ready=1、outputs_known=1及初始fill=0/drain=0，网页还须设备真实在线才可开启。WSS可选证书配置和999天网页登录token保留；以下较旧记录不代表本版输出仍禁用。

## 2026-09-12 当前：手动开关（0.7.1）

按用户最新要求，先提供无需水位传感器的补水、冲水开关。点击开启，设备回报后显示已打开，再点同一开关关闭。关闭使用STOP，因两路互锁，正常状态最多一路开启；另一只开关须等当前关闭后才能打开。独立停止输出按钮保留。

默认water_config.mode为manual，手动FILL/DRAIN不检查水位，也不读取need_fill引脚。未配置输出GPIO时仍显示等待配置；以后只需确认两路输出与启停电平，无需先安装液位板。0.8.1默认补水上限180秒、排水上限300秒，超时锁存故障；远程活动连接异常也会尝试停止，重新连接不会自动开水。

网页仅对0.7.0或0.7.1/manual启用手动开启，防止向旧自动固件发送意图不同的FILL；旧版STOP仍可用。STATUS新增control_mode，need_fill在manual模式保持unknown。API和固件支持旧automatic模式，但当前网页不显示自动换水入口。

已完成152项Lua、46项Python及本地Edge无头浏览器联调。此阶段按用户要求不做实际水流验证，也不把缺少泵或传感器作为软件交付前提。用户15:03日志已确认实板0.7.0/manual/UNCONFIGURED；0.7.1增强重连包已准备，本轮尚未刷入。

## 历史：补水和冲水独立操作（0.6.0）

控制区提供两个独立按钮：**补水**发送FILL，有补水请求时补到高位；**冲水**发送DRAIN，无补水请求时排到低位并停止，本次不自动补水。“完整换水”折叠入口仍发送START，执行排水→间隔→补水。停止输出随时优先，所有动作仍需设备在线、接线映射确认和稳定液位反馈；未配置时不启用按钮。

DRAIN要求板端0.6.0，旧版显示“更新设备后可用”，API及USB网关也拒绝新命令。该版本保留0.5.3的时基和心跳修正。138项Lua、42项Python及隔离Edge浏览器联调通过；浏览器回执使用模拟设备，不证明现场泵动作。

真实板端10:20:41启动0.5.3，COM4实读确认UNCONFIGURED；SIM为CPIN:READY，持续waiting_pdp，尚未完成新版稳定联网。电脑TLS1.2验证和公网API正常。0.6.0生成包已准备，未刷入；本轮computer-use缺少执行接口，未操作界面。最新详细状态见MEMORY/AGENTS，下方0.5.x为历史记录。

## 历史：0.5.3计时修正与夜间联调

真实板端0.5.2已通过4G/TLS/WSS认证，服务器收到了UNCONFIGURED状态；向真实板下发的一条STOP已回执 `OK STOP stopped`。原“等待设备连接”的直接原因是LuaTools选了src项目，联网关闭、设备密钥为空。改用生成包后可连接，但旧tick换算导致本应1秒的心跳拖到约80秒，先被服务器75秒超时断开。不能把短暂online描述成稳定运行。

0.5.3统一按Air724接口规定每tick为5ms计算心跳、命令有效期、控制器防抖/排补超时、HTTP升级超时及稳定连接退避。同时允许仍有效的较早心跳回执，避免4G回包晚于下一次ping时被误判；每约5秒打印seq、ack和回执年龄。依据：[官方rtos接口](https://doc.openluat.com/wiki/21?wiki_page_id=2247)。V2.4.4旧patch.lua中的 `/16` 不能作为本平台时基，旧记录以本节更正为准。

修正版已通过130项Lua、39项Python测试与LuaFLOAT语法检查。生成目录仍为 `build/firmware`，本机新项目 `water-online-0.5.3` 已写入工具project目录，需“刷新列表”后选择。用户本次已授权助手自行刷写测试，覆盖之前由用户点击的限制；computer-use在检查浏览器网址时停止了本轮界面操作，因此0.5.3尚未下载，仍缺修正版连续心跳及断线恢复的实板验证。详情见[联调记录](verification-20260912.md)。

## 2026-09-12 前次排查记录（0.5.2，以下版本快照属历史）

本机 COM4 已读到真实板端 `water_auto_exchange 0.3.0`、`UNCONFIGURED`；该版本没有 4G/WSS 功能，网页等待设备连接符合当前状态。公网 HTTPS/WSS 握手与现有设备密钥 probe 通过，服务器尚无设备上报，未发送控制命令。

0.5.2 增加本板网络指示灯 `netLed.setup(true, pio.P0_12)`，GPIO12/物理53脚专用于该灯，不能配置为泵或液位输入。网络启用时初始化灯，未启用时保持不配置 GPIO。灯表示移动网络/socket 状态；WSS 认证成功须以 `WATER WS online` 和网页真实上报为准。

本机已准备 LuaTools 项目 `water-online-0.5.2`，指向 `build/firmware/` 中的完整 9 项；关闭并重新打开 LuaTools 后选择它，再由用户点击“下载脚本”。服务器已部署该版本兼容，包内 CA 对当前站点的 TLS1.2/域名校验通过。

新版打印 `waiting_pdp`、`connecting_tls`、`upgrading_http`、`upgraded; authenticating` 及失败阶段；`WATER NET disabled` 表示下载了未启用网络的源码配置，应使用生成包。源码不含设备密钥。当前下载目标 **0.5.2**，清单仍为生成目录中的 8 个 Lua 文件和 1 个 CA，按下述准备步骤操作。控制 GPIO 保持禁用；除明确的网络灯外不配置输出。下方 0.5.1/0.2.8 为此前版本记录，不能替代本次 COM4 实读。

设备通过 Air724UG 自带 4G 网络建立 WSS 长连接，网页入口为 `https://bytegallop.com/water/`，无需电脑常开。网页提供管理密钥登录、在线状态、补水请求、软件输出、故障说明、START / FILL / DRAIN / STOP / RESET 和最近 60 条操作记录。当前为单设备控制台，不配置未经确认的定时换水或自动补水规则。

## 软件与实板边界

当前源码0.8.1、最近实板上报为0.8.0，详情以上方最新记录为准。本版启用GPIO23补水和GPIO5排水，关断沿用用户测过的写LOW后释放流程，GPIO12仍专用于网络灯。未刷入本版或未进行实板输出操作前，不把模拟测试或网站在线写成实物验收完成。

## 管理员登录

服务器凭据集中保存在 `/apps/water-auto-exchange/config/water.env`，Docker 迁移保留已有两个独立密钥：`WATER_ADMIN_KEY` 用于网页登录，`WATER_DEVICE_KEY` 用于设备认证。该文件权限为 600，禁止提交 Git。网页登录 token 的服务端有效期为登录后 **999 天（86,313,600 秒）**，固定到期，不因轮询状态而延长。会话摘要及到期时间持久化到 `data/water.db` 的 `admin_sessions` 表，正常部署和容器重启后保留；数据库不保存可直接登录的明文 token。退出登录立即撤销当前 token；更换管理密钥并重启后撤销旧密钥下的所有会话。

Cookie 下发 `Max-Age=86313600`，继续使用 Secure / HttpOnly / SameSite=Strict；浏览器自身的保存限制或清理 Cookie 可能要求提前登录。变更请求校验 Origin，登录入口每分钟最多 10 次尝试。首次从旧版内存会话升级后需重新登录一次，新会话才具有上述持久化和有效期。设备密钥的有效期及设备命令时限不受此修改影响。

本轮提供的本地登录说明位于被 Git 忽略的 `build/console-access.private.txt`，设备配置位于 `build/device.private.json`。这些文件含凭据，请勿分享或提交；聊天记录和日志不打印密钥。

## 准备 4G 固件

1. 准备可联网的 SIM、天线和供电；本版开机写LOW并释放两路输出，先核对待机状态。GPIO23 复用 SIM 在位检测的硬件约束继续有效，不能据此猜测泵映射。
2. 本轮已提供 `build/device.private.json`。后续使用自己的 JSON 文件，格式为 `{"url":"https://bytegallop.com/water","device_key":"填入设备密钥"}`。
3. 在仓库运行 `python tools/build-firmware.py --config build/device.private.json`。脚本复制源码到 `build/firmware/`，仅在该目录启用联网并填入设备密钥，源文件与 GPIO 配置保持原状。
4. 本机LuaTools“刷新列表”后选 `water-online-0.8.1`。其他机器按 `build/firmware/flash-files.txt` 创建项目并加入 **8个Lua文件和1个CA证书**，全部来自同一个生成目录。保留现有CORE、默认LuaTask V2.4.4库及USB trace。每条require独占一行；JSON为CORE内置全局模块。
5. 由用户点击“下载脚本”（此前Escape停止的UI操作尚未恢复）。下载后核对 `project=water_auto_exchange version=0.8.1`、`manual`、`ready=1`、`outputs_known=1`、初始两路为0和生成配置，并查看服务器是否收到真实上报。持续观察至少跨过原75秒断线窗口，确认5秒摘要中的心跳seq/ack按待机30秒、活动1秒继续增长、服务器last_seen持续刷新，再验证重连。仅版本号或一次online不足以通过联调。
6. 手动模式只需确认输出启停、输出接线和超时参数后修改 `src/water_config.lua`；不要求水位传感器。自动模式另需液位板隔离反馈接线，重新生成整包、验证、提交并推送，再由用户刷写。不能直接在生成目录内做长期配置修改，重新生成会覆盖该目录文件。

下载清单：`main.lua`、`water_config.lua`、`water_cycle.lua`、`water_control.lua`、`water_usb.lua`、`water_network.lua`、`water_network_config.lua`、`water_ws_transport.lua`、`water-ca.crt`。

TLS强制提供CA文件并开启SNI。CA来源与指纹见[证书说明](../certs/README.md)。0.5.2实板TLS与WSS认证已通过，0.5.3持续连接仍待验证。禁止通过删除CA配置绕过校验。网络接口按本机V2.4.4 `sys.lua` / `socket4G.lua` 核对：connect/send超时单位为秒，recv为毫秒；Air724的 `rtos.tick()` 每计数为5ms。

## WSS 通信与操作语义

设备连接 `wss://bytegallop.com/water/api/device/ws`。服务端主动推送命令，设备空闲每 1 秒发送小型应用心跳；状态变化立即上报。换水期间每 1 秒发送小心跳，10 秒收不到服务器有效回复会尝试停止远程启动的周期。网页自身的 HTTP 刷新发生在浏览器与服务器之间，不触发设备轮询。

- 状态不变时不重复上传完整状态，不自动回退到 HTTP 轮询。断线立即标记离线，半开连接空闲最多 75 秒判离线；活动状态最多 10 秒。
- 握手后先认证，设备密钥只在 TLS 加密消息中传送，不放 URL；认证前不读取设备状态、不交付命令。诊断 `probe` 只检查鉴权，不注册设备或更改线上记录。
- 命令有效期仍为 8 秒。服务器推送 offer，设备发送 claim 并记录本地单调时钟；服务器检查剩余有效期后返回 execute。设备再检查 claim 往返耗时与剩余有效期，避免延迟缓存的旧命令启动输出。命令交付、执行与设备回执分别记录，回执丢失显示“结果待核实”，不自动重发 START/FILL/DRAIN。
- START/FILL/DRAIN/RESET 仍由原控制器检查液位、防抖、互锁、超时和故障。STOP 优先，取消仍排队的命令；设备收到 STOP offer 会清除尚未执行的其他 claim。
- USB STOP 会作废在途远程响应并关闭当前连接；新连接创建新会话，旧命令不能恢复泵。远程 STOP 保留连接以便立即回传结果。
- `water_ws_transport.lua` 的单一任务持有 socket；connect/send/recv/close 直接在该任务中执行，不放进外层 pcall。业务调用 send 只入队，不直接等待底层 SOCKET_SEND。
- 收发支持 TCP 分包、粘包、126 字节边界、WebSocket 分片及 Ping/Pong。每条消息上限 8192 字节，发送队列上限 8 条；异常输入、队列失败及超时会断线并尝试关闭远程输出。应用/传输层不打印认证消息，认证固定前缀避免底层 socket 调试预览包含密钥。
- 0.7.1起已支持：已注册时连续6次连接失败，或等待IP超过120秒，调用当前LuaTask库的link.shut恢复链路就绪状态；每次至少间隔300秒。未注册时等待底层搜网，不循环切飞行模式。故障期间每60秒最多一批只读AT诊断，IP_ERROR立即作废远程会话并尝试停止其输出。详见[网络恢复与现场注册诊断](network-recovery.md)。
- 失败重连按 1、2、4 秒逐步延长，最多 60 秒；连接稳定达到 60 秒后重置退避。连接失败和身份拒绝不会每秒重新握手。不可恢复的任务异常或时钟异常停止网络控制，需排查后重启。
- 初期 TLS 握手依然有流量开销。长连接避免每 2 秒重复 TCP/TLS/HTTP 请求；实际运营商流量仍需实板测量，不据模拟测试填写节省百分比。
- TLS 必须携带 CA、SNI，并设置 `insist=0` 拒绝证书域名校验失败。板端实际 TLS 握手与长时间在线仍待用户刷写验证。
- 网络 STOP 写入失败会打印 `stop_unconfirmed`，硬件适配器继续显示输出不确定或故障；软件无法替代独立硬件断流。

## USB 调试网关（可选）

独立 4G 是当前主接入方式。USB 工具用于本地调试，支持 0.3.0/0.5.1 协议；不要与板端 4G 同时连接同一控制台，也不要与 LuaTools/其他串口工具争用用户口。

```powershell
python -m pip install -r tools/requirements-gateway.txt
python tools/water-gateway.py --port COM4 --config build/device.private.json
```

先重新枚举并确认用户串口。网关启动、断网或退出时尝试 STOP；仅在识别出正确项目和支持版本后发送控制命令。当前 0.5.1 纯 USB 主机命令使用 `tools/water-command.ps1`。

## 验证

```powershell
python -m pip install -r server/requirements.txt websocket-client
python -m unittest discover -s tests -p "test_web*.py"
# 以下测试工具可安装到 .venv，避免改变全局环境。
python -m pip install lupa playwright
python tools/test-lua.py
python tests/web_browser_test.py
```

浏览器测试使用本机 Edge 和隔离的模拟设备状态，不连接真实串口。检查登录、鉴权、状态刷新、命令回执、停止、移动端和转义。模拟测试不构成实机换水验证。
